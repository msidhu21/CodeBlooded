Testing Evidence - Milestone 3

This folder contains testing proof for User Story 1: User Registration.

- All unit and integration tests for `AuthService` passed successfully.
- See `pytest_results.png` for the test output evidence.
- Tested functionalities:
  - User registration with unique email
  - Duplicate email check
  - Missing information validation

Each test passed successfully using `pytest` in the backend environment.

